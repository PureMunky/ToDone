'use strict';

var todo = require('../server/todo/todoModel.js'),
  mongoose = require('mongoose');


describe('todoModel.js', function () {

  //it('works', function () {
  //  expect(1).toBe(1);
  //});
  
  //it('can get todos', function () {
  //  var task = new todo({ title: 'test title' });
  //  expect(task.get).toBeDefined();
  //});

  //it('gets todos', function (done) {
  //  var Task = new todo({ id: 123, title: 'test title' });
  //  Task.save();
  //  todo.find({ title: 'test title' }, function (err, tasks) {
  //    expect(tasks.length).toBeGreaterThan(0);
  //    done();
  //  });

  //});

});